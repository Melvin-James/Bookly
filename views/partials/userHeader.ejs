<header class="bg-theme text-white px-6 py-4 shadow flex justify-between items-center">
  <h1 class="text-xl font-semibold tracking-wide">User Dashboard</h1>
  
  <div class="flex items-center gap-4">
    <span class="hidden md:block">Welcome, <%= userData.name %></span>
    <img src="/uploads/profileImages/<%= userData.userImage[0] %>" alt="Profile" class="w-10 h-10 rounded-full object-cover ring-2 ring-white shadow" />
  </div>
</header>
