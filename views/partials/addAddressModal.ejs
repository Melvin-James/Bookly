<!-- Add Address Modal -->
<div id="addAddressModal" class="fixed inset-0 z-50 hidden bg-black/30 backdrop-blur-sm flex items-center justify-center">
  <div class="bg-white w-full max-w-3xl rounded-xl shadow-xl p-6 relative animate-fade-in">
    <button class="absolute top-3 right-4 text-gray-600 hover:text-gray-800 text-xl" onclick="toggleAddressModal()">
      <i class="fas fa-times"></i>
    </button>
    
    <!-- Include the existing content here -->
    <%- include('partials/addAddressContent', { address, formAction, userData, error }) %>
  </div>
</div>
