<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Bookly</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }

        .login-container {
            width: 100%;
            max-width: 420px;
        }

        .brand-section {
            text-align: center;
            margin-bottom: 2rem;
        }

        .brand-icon {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 64px;
            height: 64px;
            background: #007D8B;
            border-radius: 16px;
            margin-bottom: 1rem;
            box-shadow: 0 10px 25px rgba(0, 125, 139, 0.25);
        }

        .brand-icon i {
            font-size: 32px;
            color: white;
        }

        .brand-title {
            font-size: 2rem;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }

        .brand-subtitle {
            color: #64748b;
            font-size: 1rem;
        }

        .login-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.3);
            overflow: hidden;
        }

        .card-header {
            padding: 2rem 2rem 1rem;
            text-align: center;
            background: linear-gradient(135deg, rgba(0, 125, 139, 0.05) 0%, rgba(0, 125, 139, 0.02) 100%);
        }

        .card-title {
            font-size: 1.75rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }

        .card-description {
            color: #64748b;
            font-size: 0.9rem;
        }

        .card-content {
            padding: 1.5rem 2rem 2rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            font-size: 0.875rem;
            font-weight: 600;
            color: #374151;
            margin-bottom: 0.5rem;
        }

        .form-input {
            width: 100%;
            height: 48px;
            padding: 0 1rem;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: white;
            color: #1e293b;
        }

        .form-input:focus {
            outline: none;
            border-color: #007D8B;
            box-shadow: 0 0 0 4px rgba(0, 125, 139, 0.1);
            transform: translateY(-1px);
        }

        .form-input::placeholder {
            color: #94a3b8;
        }

        .form-input.error {
            border-color: #ef4444;
            background: #fef2f2;
        }

        .error-message {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: #dc2626;
            font-size: 0.875rem;
            margin-top: 0.5rem;
            font-weight: 500;
        }

        .error-message i {
            font-size: 0.75rem;
        }

        .login-button {
            width: 100%;
            height: 48px;
            background: linear-gradient(135deg, #007D8B 0%, #005F66 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 125, 139, 0.3);
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .login-button:hover {
            background: linear-gradient(135deg, #005F66 0%, #004A52 100%);
            box-shadow: 0 6px 20px rgba(0, 125, 139, 0.4);
            transform: translateY(-2px);
        }

        .login-button:active {
            transform: translateY(0);
        }

        .links-section {
            text-align: center;
            font-size: 0.875rem;
        }

        .link-item {
            margin-bottom: 0.75rem;
        }

        .link-item:last-child {
            margin-bottom: 0;
        }

        .link {
            color: #007D8B;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .link:hover {
            color: #005F66;
            text-decoration: underline;
        }

        .footer {
            text-align: center;
            margin-top: 2rem;
            color: #64748b;
            font-size: 0.875rem;
        }

        /* Input focus animations */
        .form-group {
            position: relative;
        }

        .form-input:focus + .form-label,
        .form-input:not(:placeholder-shown) + .form-label {
            transform: translateY(-8px) scale(0.85);
            color: #007D8B;
        }

        /* Responsive Design */
        @media (max-width: 480px) {
            body {
                padding: 0.5rem;
            }

            .login-container {
                max-width: 100%;
            }

            .brand-title {
                font-size: 1.75rem;
            }

            .card-header,
            .card-content {
                padding-left: 1.5rem;
                padding-right: 1.5rem;
            }

            .brand-icon {
                width: 56px;
                height: 56px;
            }

            .brand-icon i {
                font-size: 28px;
            }

            .card-title {
                font-size: 1.5rem;
            }
        }

        @media (max-width: 360px) {
            .card-header,
            .card-content {
                padding-left: 1rem;
                padding-right: 1rem;
            }
        }

        /* Subtle animations */
        .login-card {
            animation: slideUp 0.6s ease-out;
        }

        .brand-section {
            animation: fadeIn 0.8s ease-out;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* Enhanced visual hierarchy */
        .form-input:focus {
            box-shadow: 0 0 0 4px rgba(0, 125, 139, 0.1), 0 4px 12px rgba(0, 125, 139, 0.15);
        }

        .login-button {
            position: relative;
            overflow: hidden;
        }

        .login-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .login-button:hover::before {
            left: 100%;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <!-- Login Card -->
        <div class="login-card">
            <div class="card-header">
                <h2 class="card-title">Bookly Login</h2>
                <p class="card-description">Enter your credentials to access your account</p>
            </div>

            <div class="card-content">
                <% if (errors?.login) {%>
                    <div class="error-message login-error">
                        <i class="fas fa-exclamation-circle"></i>
                        <span><% errors.login.msg %></span>
                    </div>
                    <% } %>
                <form id="userLoginForm" method="POST" action="/user/login">
                    <!-- Email Field -->
                    <div class="form-group">
                        <label for="email" class="form-label">Email Address</label>
                        <input 
                            type="email" 
                            id="email" 
                            name="email" 
                            class="form-input <%= errors?.email ? 'error' : '' %>"
                            placeholder="Enter your email"
                            value="<%= formData?.email || '' %>"
                        
                        >
                        <% if (errors?.email) { %>
                            <div class="error-message">
                                <i class="fas fa-exclamation-circle"></i>
                                <span><%= errors.email.msg %></span>
                            </div>
                        <% } %>
                    </div>

                    <!-- Password Field -->
                    <div class="form-group">
                        <label for="password" class="form-label">Password</label>
                        <input 
                            type="password" 
                            id="password" 
                            name="password" 
                            class="form-input <%= errors?.password ? 'error' : '' %>"
                            placeholder="Enter your password"

                        >
                        <% if (errors?.password) { %>
                            <div class="error-message">
                                <i class="fas fa-exclamation-circle"></i>
                                <span><%= errors.password.msg %></span>
                            </div>
                        <% } %>
                    </div>
                    <!-- Submit Button -->
                    <button type="submit" class="login-button">
                        Sign In
                    </button>

                    <!-- Links -->
                    <div class="links-section">
                        <div class="link-item">
                            <a href="/user/forgot-password" class="link">Forgot your password?</a>
                        </div>
                        <div class="link-item">
                            <span style="color: #64748b;">Don't have an account? </span>
                            <a href="/user/signup" class="link">Sign up here</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>&copy; 2025 Bookly. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
